# 🔮 오늘의 타로 기능 — 개발 안내서

소소다이어리(Soso Diary) 앱에 추가된 **"오늘의 타로"** 기능에 대한 정리 문서입니다.
누가 봐도 이해할 수 있도록 쉽게 적었습니다.

- 개발 브랜치: `claude/tarot-app-dev-h9lhzg`
- 작업일: 2026-08-05

---

## 1. 한 줄 요약

기존 **소소다이어리 앱 안에** '오늘의 타로' 기능을 새로 넣었습니다.
홈 화면에서 **🔮 오늘의 타로** 버튼을 누르면, 타로 카드를 뽑고 해석을 볼 수 있습니다.
(별도의 앱이 아니라, 다이어리 앱의 한 기능입니다.)

---

## 2. 무엇을 할 수 있나요? (기능 소개)

1. **스프레드 선택** — 두 가지 방식 중 고를 수 있어요.
   - `오늘의 카드` : 카드 **1장**으로 오늘의 메시지를 받아요.
   - `과거·현재·미래` : 카드 **3장**으로 흐름을 읽어요.
2. **카드 섞기 연출** — 카드를 섞는 애니메이션이 잠깐 나와요.
3. **카드 뒤집기** — 뒷면 카드를 누르면 3D로 뒤집히며 앞면이 보여요.
   (`한 번에 모두 펼치기` 버튼으로 한꺼번에 열 수도 있어요.)
4. **해석 보기** — 카드마다 이름·키워드·해석 글이 나와요.
   - 카드는 **정방향/역방향**으로 나오고, 방향에 따라 해석이 달라져요.
5. **다시 뽑기** — 처음으로 돌아가 다시 뽑을 수 있어요.

> 💡 타로 카드는 **총 78장**(메이저 22장 + 마이너 56장) 전부 들어 있고,
> 모두 한글 이름과 해석이 달려 있습니다.
> 로그인이나 인터넷 서버 없이 **화면에서 바로** 동작합니다.

---

## 3. 어디를 눌러야 하나요? (화면 위치)

홈 화면에서 아래 위치에 버튼이 생깁니다.

```
📅 2026년 8월 5일 수요일
소소님, 오늘의 한 줄을 남겨보세요 🍀

[✏️ 오늘 일기 쓰기]   [💰 소비·수입 기록]

┌──────────────────────────────────┐
│ 🔮  오늘의 타로               →   │  ← 여기! (버튼 두 개 바로 아래)
│     카드 한 장으로 마음을 들여다봐요 │
└──────────────────────────────────┘

┌─ 오늘의 명언 ───────────────────┐
   "가장 좋은 거울은 오랜 친구이다."
   ...
```

즉, **"소비·수입 기록" 버튼과 "오늘의 명언" 카드 사이**에 나타납니다.

---

## 4. 어떤 파일이 만들어졌나요? (개발자용)

프로젝트 폴더(`diary`) 안에서 아래 파일들이 추가·수정되었습니다.

### ✨ 새로 만든 파일 (4개)

| 파일 | 하는 일 |
| --- | --- |
| `src/config/tarot.ts` | 타로 카드 **78장 데이터** (이름·키워드·정방향/역방향 해석) |
| `src/lib/tarot.ts` | 카드 **섞기·뽑기 로직** (정/역방향 랜덤) |
| `src/pages/TarotPage.tsx` | 타로 **화면(UI)** — 스프레드 선택·섞기·뒤집기·해석 |
| `src/pages/tarot.css` | 타로 화면 **전용 디자인(스타일)** |

### 🔧 고친 파일 (3개)

| 파일 | 무엇을 고쳤나 |
| --- | --- |
| `src/pages/HomePage.tsx` | 홈에 **🔮 오늘의 타로 버튼** 추가 |
| `src/pages/home.css` | 그 버튼의 **디자인** 추가 |
| `src/components/AppShell.tsx` | 버튼을 누르면 **타로 화면으로 연결** |

---

## 5. 화면에서 확인하는 방법 (아주 쉽게)

> ℹ️ 타로 기능은 `claude/tarot-app-dev-h9lhzg` 라는 **새 버전(브랜치)** 에 담겨 있어요.
> 이 새 버전을 컴퓨터로 받아서 실행하면 타로 버튼이 화면에 나타납니다.

### 준비물 (처음 한 번만)

- **Git** : https://git-scm.com/download/win 에서 설치
- **Node.js** : https://nodejs.org 에서 `LTS` 버튼 눌러 설치

### 순서 (Windows 기준)

1. **명령어 입력 창**을 엽니다.
   `Windows 키` → `powershell` 이라고 검색해서 실행.

2. 타로 버전을 넣을 폴더로 이동합니다. (예: `D:\03.tarot`)
   ```powershell
   cd /d D:\03.tarot
   ```

3. 프로젝트를 내려받습니다. *(폴더에 처음 받는 경우)*
   ```powershell
   git clone https://github.com/sj01040345083-ux/diary.git .
   ```
   > 맨 끝의 `.`(점)은 "이 폴더 안에 바로 받기"라는 뜻이에요.

4. 타로 버전으로 바꿉니다.
   ```powershell
   git checkout claude/tarot-app-dev-h9lhzg
   ```

5. 필요한 부품을 설치하고 실행합니다.
   ```powershell
   npm install
   npm run dev
   ```

6. 화면에 나오는 주소(`http://localhost:5173`)를
   **크롬 브라우저 주소창**에 붙여넣고 엽니다.
   → 홈 화면에 **🔮 오늘의 타로** 버튼이 보이면 성공! 🎉

### 이미 폴더에 프로젝트가 있는 경우

새로 받지 않고, 최신 내용만 갱신하려면:
```powershell
git checkout claude/tarot-app-dev-h9lhzg
git pull
npm install
npm run dev
```

> 💡 이미 `npm run dev`가 켜져 있었다면, 껐다가 다시 켠 뒤
> 브라우저를 **새로고침(F5)** 해야 새 버튼이 보입니다.

---

## 6. 자주 나오는 궁금증

**Q. 타로는 다이어리랑 따로 있는 앱인가요?**
A. 아니요. **다이어리 앱 안의 한 기능**입니다. 그래서 폴더 하나(`diary`)에 전부 들어 있어요.

**Q. 인터넷(서버)이나 로그인 없이도 타로가 되나요?**
A. 네. **타로 기능 자체는 화면에서 바로** 동작합니다.
   (다만 일기 저장·로그인 같은 다른 기능은 Supabase 연결 설정 `.env`가 필요해요.)

**Q. 카드 그림(일러스트)도 있나요?**
A. 지금은 카드마다 **이모지 상징**으로 표현했어요. 원하시면 실제 그림으로 바꿀 수 있어요.

---

## 7. 앞으로 더 할 수 있는 것 (선택)

- 뽑은 타로 카드를 **그날 일기에 저장**하기 (운세 기록 남기기)
- **켈틱 크로스** 등 더 큰 스프레드 추가
- 카드에 **실제 일러스트 이미지** 적용
- 하단 탭바에 **🔮 타로 탭** 고정 추가

필요하신 게 있으면 말씀해 주세요. 하나씩 쉽게 도와드릴게요! 😊
