# 🍃 오늘의 한 장 — 설정 안내 (Phase 1)

명세대로 만든 **"오늘의 한 장"**(하루 한 장 · 일기 트리거형 타로) 기능입니다.
기존 **🔮 오늘의 타로**(운세형)는 그대로 두고, 이 기능이 **따로** 추가되었습니다.

---

## 1. 이 기능이 뭔가요?

- 하루에 **딱 한 장**만 뽑습니다. (다시 뽑기 불가)
- 카드는 운세를 **예언하지 않아요.** 대신 **일기 질문**을 건넵니다.
  예) 별 카드 → “오늘 조금이라도 나아졌다고 느낀 게 있나요?”
- 그 질문을 들고 **바로 일기 쓰기**로 이어집니다.
- 일기를 안 써도 **카드 기록은 남습니다.** (나중에 캘린더 연동 예정 — Phase 2)
- 같은 날 다시 들어와도 **같은 카드**가 나옵니다. (사람+날짜로 결정)

---

## 2. ⚠️ 먼저 해야 할 일 — DB 테이블 만들기 (한 번만)

이 기능은 카드 기록을 저장할 **새 테이블**이 필요해요.
**Supabase 대시보드에서 아래 순서대로 딱 한 번만** 해주시면 됩니다.

1. https://supabase.com 접속 → 로그인 → 이 앱의 프로젝트 선택
2. 왼쪽 메뉴에서 **SQL Editor**(종이에 `>_` 모양 아이콘) 클릭
3. **New query** 버튼 클릭
4. 저장소의 **`supabase/daily_cards.sql`** 파일 내용을 **전부 복사**해서 붙여넣기
5. 오른쪽 아래 **Run**(실행) 버튼 클릭
6. 아래쪽에 `Success` 라고 나오면 끝! ✅

> 이 SQL은 여러 번 실행해도 안전하게 만들어져 있어요(이미 있으면 건너뜀).
> 테이블을 안 만들면, 카드는 화면에 보이지만 **기록이 저장되지 않아요.**

---

## 3. 화면에서 보는 법

1. 최신 코드 받기
   ```powershell
   git checkout main
   git pull
   npm install
   npm run dev
   ```
2. 앱 홈에서 **🍃 오늘의 한 장** 버튼을 누르세요.
   (🔮 오늘의 타로 바로 아래에 있어요)
3. 카드를 뽑고 → 질문을 확인 → **“이 물음으로 일기 쓰기”** 로 이어집니다.

---

## 4. 지금 된 것 / 앞으로 할 것

**이번(Phase 1)에 된 것**
- 하루 한 장, 다시 뽑기 불가 (DB 제약으로 강제)
- 같은 날 재접속 시 같은 카드
- 메이저 아르카나 22장 (질문 3개 + 역방향 문구 1개씩)
- 카드 질문을 들고 일기 작성으로 이동 (질문은 상단 인용구로만, 본문 강제 입력 없음)
- 일기를 안 써도 카드 기록은 남음

**다음 단계 (아직 안 함)**
- Phase 2: 감정 캘린더('한 달의 숲')에 카드 함께 표시 + 월간 통계
- Phase 3: 매월 말 회고형 3장 스프레드

---

## 5. 참고

- 화면 디자인은 앱 기존 '이끼와 안개' 톤을 그대로 썼어요. (금박·별 같은 장식 없이 차분하게)
- 카드 그림은 저작권 문제를 피하려고 **이미지 없이 카드명 + 키워드 타이포**로 표현했어요.
  (원하시면 나중에 직접 그린 일러스트로 교체 가능)
