# 소소 Diary — 프로젝트 종합 가이드 🌿

> 이 문서 하나만 보면 **프로젝트를 처음부터 다시 세팅하고, 이어서 개발**할 수 있도록
> 정리한 마스터 문서입니다. (최초 생성 ~ 타로 기능까지)
>
> 마지막 업데이트: 2026-08-05

---

## 0. 한눈에 보기

- **무엇**: 하루의 감정과 소소한 지출을 함께 적는 개인용 다이어리 웹앱 (모바일 우선)
- **누구**: 로그인한 본인만 자기 데이터를 봄 (Supabase 인증 + RLS)
- **핵심 기능**: 일기 · 사진 · 지출/수입 가계부 · 통계 · 오늘의 명언(즐겨찾기) · **타로**
- **저장소(GitHub)**: `github.com/sj01040345083-ux/diary`
- **개발 브랜치**: `claude/tarot-app-dev-h9lhzg` → PR → `main` 병합
- **로컬 폴더(내 PC)**: `D:\1.AI_Workspace folders\01.Code_First_Project\03.tarot`
  (⚠️ 예전 `...\01.diary 웹.앱 개발\diary` 폴더는 **옛날 복사본**이라 사용하지 않음)

---

## 1. 기술 스택

| 구분 | 사용 기술 |
|---|---|
| 프론트엔드 | React 19 + TypeScript |
| 빌드/개발서버 | Vite 8 |
| 백엔드(BaaS) | Supabase (Auth · PostgreSQL · Storage) |
| 린트 | oxlint |
| 내보내기 | `docx`(워드), `xlsx`(엑셀) |
| 스타일 | 순수 CSS (`src/index.css`, `src/pages/*.css`) |

- 상태관리 라이브러리·라우터 없음. **화면 전환은 `useState`로 직접 관리** (App.tsx / AppShell.tsx).
- 별도 서버 코드 없음. 데이터는 전부 Supabase가 담당.

---

## 2. 처음부터 실행하는 법 (새 PC / 다시 시작)

```powershell
# 1) 저장소 받기 (이미 폴더가 있으면 생략)
git clone https://github.com/sj01040345083-ux/diary.git
cd diary

# 2) 최신 코드로 맞추기
git checkout main
git pull

# 3) 패키지 설치
npm install

# 4) 환경변수 파일 만들기 (.env.example 복사)
#    .env 파일을 만들고 아래 두 값을 채웁니다 (3번 항목 참고)
#    VITE_SUPABASE_URL / VITE_SUPABASE_PUBLISHABLE_KEY

# 5) 개발 서버 실행
npm run dev
#   → 브라우저에서 http://localhost:5173 접속

# (선택) 빌드 / 미리보기 / 린트
npm run build
npm run preview
npm run lint
```

> ⚠️ 서버를 켠 상태에서 `.env`를 바꾸면 **개발 서버를 껐다 다시 켜야** 반영됩니다.

---

## 3. 환경변수 (.env)

`.env` 파일(깃에 올라가지 않음)에 아래 2개를 넣습니다.

```env
VITE_SUPABASE_URL=https://xxxxx.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=<Supabase publishable(공개용) 키>
```

- Supabase 대시보드 → Project Settings → API 에서 확인.
- **publishable/anon(공개) 키만** 사용합니다. `service_role`(비밀) 키는 프론트엔드·공개 저장소에 **절대 넣지 않습니다.**
- 이 값이 없으면 앱이 시작 시 에러를 띄웁니다 (`src/lib/supabase.ts`).

---

## 4. 폴더 구조

```
diary/
├─ index.html                # 앱 진입 HTML
├─ package.json              # 스크립트/의존성
├─ vite.config.ts            # Vite 설정
├─ tsconfig*.json            # TypeScript 설정
├─ .env.example              # 환경변수 예시 (복사해서 .env 만들기)
├─ quotes.json               # (원본 명언 데이터 참고용)
├─ public/                   # 정적 파일
├─ src/
│  ├─ main.tsx               # React 앱 부트스트랩
│  ├─ App.tsx                # 로그인 여부에 따라 화면 분기 (인증 게이트)
│  ├─ index.css              # 전역 스타일
│  ├─ assets/backgrounds/    # 배경 이미지 (1~6.jpg)
│  ├─ components/            # 공통 UI 조각
│  │   ├─ AppShell.tsx       #   로그인 후 전체 틀 (탭/오버레이 화면 관리)
│  │   ├─ TabBar.tsx         #   하단 탭바 (홈/기록/통계/설정)
│  │   ├─ Header.tsx
│  │   ├─ AuthLayout.tsx     #   로그인/회원가입 공통 레이아웃
│  │   ├─ DiaryCalendar.tsx  #   기록 탭 달력
│  │   ├─ DatePickerCalendar.tsx
│  │   ├─ DiaryPhotos.tsx / PhotoCollage.tsx  # 사진
│  ├─ config/                # "데이터/설정" (여기만 고치면 내용이 바뀜)
│  │   ├─ tarot.ts           #   ★ 타로 78장 카드 데이터
│  │   ├─ quotes.ts          #   명언 데이터
│  │   ├─ emotions.ts        #   기분 이모지 목록
│  │   ├─ categories.ts      #   기본 가계부 카테고리
│  │   ├─ backgrounds.ts     #   배경 목록
│  │   ├─ theme.ts / appConfig.ts
│  ├─ lib/                   # "동작 로직" (Supabase 통신 등)
│  │   ├─ supabase.ts        #   Supabase 연결 통로
│  │   ├─ tarot.ts           #   ★ 타로 뽑기/해석 엔진
│  │   ├─ diaries.ts         #   일기 CRUD
│  │   ├─ transactions.ts    #   가계부 CRUD
│  │   ├─ photos.ts          #   사진 업로드(Storage)
│  │   ├─ favorites.ts       #   명언 즐겨찾기
│  │   ├─ categories.ts      #   사용자 카테고리
│  │   ├─ settings.ts        #   설정(닉네임/배경/폰트)
│  │   ├─ authErrors.ts      #   Supabase 에러 → 한국어 안내
│  │   ├─ autoLogin.ts       #   자동 로그인 유지 로직
│  │   ├─ exporters.ts       #   워드/엑셀 내보내기
│  │   ├─ imageCompress.ts / today.ts / favorites.ts
│  └─ pages/                 # 화면들
│      ├─ LoginPage / SignupPage / ResetPasswordPage
│      ├─ HomePage           #   홈(오늘 명언/바로가기)
│      ├─ WritePage          #   일기 작성/수정
│      ├─ RecordsPage        #   기록(달력)
│      ├─ TransactionsPage   #   가계부
│      ├─ StatsPage          #   통계
│      ├─ FavoritesPage      #   즐겨찾기 명언
│      ├─ SettingsPage       #   설정
│      ├─ TarotPage          #   ★ 타로
│      └─ *.css              #   auth.css / home.css / tarot.css
└─ (문서) 기획서.md · 소소다이어리-안내서.md · 타로-기능-안내.md · 이 문서
```

📌 **설계 원칙**: `config/` = "무엇을 보여줄지(데이터)", `lib/` = "어떻게 동작할지(로직)", `pages/` = "화면". 카드 뜻/명언/카테고리 등 **내용만 바꾸려면 `config/`만** 손대면 됩니다.

---

## 5. 백엔드(Supabase) 구조

### 5-1. 인증 (Auth)
- 이메일 + 비밀번호 로그인/회원가입.
- 비밀번호 재설정: 메일의 링크 클릭 → 앱이 `PASSWORD_RECOVERY` 이벤트 감지 → `ResetPasswordPage` 표시 (`App.tsx`).
- 자동 로그인 유지 여부는 `lib/autoLogin.ts`로 관리 (비밀번호는 저장하지 않음).

### 5-2. 데이터베이스 테이블
> 모든 테이블은 **RLS(Row Level Security)로 "본인 데이터만" 접근**하도록 설정해야 합니다.
> (정책: `auth.uid() = user_id`)

| 테이블 | 주요 컬럼 | 설명 |
|---|---|---|
| `diaries` | `id`, `user_id`, `entry_date`(날짜), `content`, `mood`(기분 이모지/null), `created_at`, `updated_at` | 하루 1개 (unique: `user_id, entry_date`, upsert) |
| `transactions` | `id`, `user_id`, `type`(수입/지출), `amount`, `category`, `memo`, `tx_date` | 가계부 |
| `categories` | `id`, `user_id`, `type`, `name` | 사용자 정의 카테고리 |
| `favorite_quotes` | `id`, `user_id`, `quote_id`, `text`, `author` | 명언 즐겨찾기 |
| `settings` | `user_id`(unique), `nickname`, `bg`(배경), `font`, `font_size`, `updated_at` | 사용자 설정 (upsert) |

### 5-3. 스토리지(Storage)
- 버킷: **`diary-photos`**
- 경로 규칙: `<사용자ID>/<날짜>/<파일명>.jpg`
- 업로드 전 `lib/imageCompress.ts`로 이미지 압축.

---

## 6. 앱 흐름 (화면 구조)

```
main.tsx → App.tsx
  ├─ 로그인 확인 중 → "불러오는 중…"
  ├─ 비밀번호 재설정 링크 진입 → ResetPasswordPage
  ├─ 로그인 안 됨 → LoginPage ↔ SignupPage
  └─ 로그인 됨 → AppShell
        ├─ 탭바: 홈 / 기록 / 통계 / 설정
        └─ 오버레이(전체화면): 일기작성 / 가계부 / 즐겨찾기 / 타로
```

- 홈(HomePage)의 바로가기 버튼으로 **가계부·즐겨찾기·타로** 오버레이를 엽니다.

---

## 7. ★ 타로 기능 상세

### 7-1. 파일
| 파일 | 역할 |
|---|---|
| `src/config/tarot.ts` | **카드 78장 데이터** (이름·이모지·키워드·정/역방향 해석). 내용만 바꾸려면 여기. |
| `src/lib/tarot.ts` | 뽑기 로직 · 주제 · **해석 생성 엔진(buildReading)** · 일기 저장 포맷 |
| `src/pages/TarotPage.tsx` | 화면(주제 선택 → 섞기 → 카드 고르기 → 결과) |
| `src/pages/tarot.css` | 타로 전용 스타일 (카드 뒷면 달·별 그림 포함) |

### 7-2. 카드 데이터 구조 (`config/tarot.ts`)
```ts
type TarotCard = {
  id: string            // 예: 'major-0', 'wands-5'
  name: string          // 한글 이름 (예: '바보')
  nameEn: string
  arcana: 'major' | 'minor'
  suit?: 'wands' | 'cups' | 'swords' | 'pentacles'
  emoji: string         // 앞면 상징
  keywords: string[]    // 핵심 키워드 2~3개
  upright: string       // 정방향 해석
  reversed: string      // 역방향 해석
}
```
- 메이저 22장 + 마이너 56장(완드/컵/소드/펜타클 각 14장) = **78장**.

### 7-3. 사용자 흐름
1. **주제 선택** (5개): 🌅 오늘의 운세 · 💗 연애·관계 · 💼 일·금전 · 🌿 마음·고민 · 👥 인간관계
   - 주제를 누르면 바로 다음(섞기) 단계로 이동.
2. **섞기 화면**: "이 주제를 몇 장으로 볼까요?" → `🃏 1장(핵심)` / `🕰️ 3장(과거·현재·미래)` 선택 → **[카드 섞기]** 버튼.
3. **카드 고르기**: 뒷면 카드 21장을 **6열 그리드**로 펼쳐서 필요한 장수만큼 선택.
4. **결과**: 고른 카드가 뒤집히며 **여러 문단 해석**이 나옴 + 일기 저장 가능.

### 7-4. 뽑기 규칙 (`lib/tarot.ts`)
- **오늘의 운세(today)**: 같은 날짜·시간대(아침/점심/저녁/밤)에는 **같은 카드**가 나오도록 시드 고정.
- **그 외 주제**: 뽑을 때마다 **매번 새 카드**(랜덤).
- 정/역방향도 무작위.

### 7-5. 해석 엔진 `buildReading()` (핵심)
카드 한 장을 **5개 섹션**으로 길게 풀어줍니다 (외부 사이트 문구 미사용, 100% 원본):

1. 🔮 **이 카드는** — 무대(메이저/슈트) + 키워드 + 정/역방향 프레이밍
2. 📖 **지금의 메시지** — 카드 고유 해석(`upright`/`reversed`)
3. `{주제} 이야기` — 주제 × 방향별 문단 (키워드를 녹여서)
4. 🧭 **조언** — 카드 종류(메이저/슈트) × 방향별
5. 🌙 **마무리** — 방향별 격려 (카드마다 변형)

- 문구 템플릿은 `lib/tarot.ts` 안의 `topicStory` / `adviceStory` / `closingStory` 객체에 있습니다. **여기 문장을 고치면 해석 톤이 바뀝니다.**
- 3장 스프레드는 각 카드가 이 5섹션을 갖고, 자리(과거/현재/미래)는 카드 위 배지로 표시.

### 7-6. 더 깊게 확장하려면 (TODO 아이디어)
- 카드 78장 각각에 **주제별 전용 장문 해석**을 직접 작성해 `topicStory`의 템플릿 대신 카드별 데이터로 교체 (콘텐츠 작업, 메이저 22장부터 단계적으로 추천).
- 카드 앞면에 실제 일러스트 이미지 추가(직접 제작/무료 라이선스 이미지만).

---

## 8. 기타 기능 요약

- **일기(WritePage)**: 날짜별 1개, 기분 이모지 + 본문 + 사진. `diaries` 테이블 upsert.
- **가계부(TransactionsPage)**: 수입/지출 기록, 카테고리·메모. `transactions` 테이블.
- **통계(StatsPage)**: 지출/기분 등 집계.
- **명언(HomePage/FavoritesPage)**: 오늘의 명언 표시 + 즐겨찾기(`favorite_quotes`).
- **설정(SettingsPage)**: 닉네임·배경·글씨체·글씨크기 (`settings`).
- **내보내기(exporters.ts)**: 워드/엑셀로 저장.
- **인증 에러 한국어화(authErrors.ts)**: Supabase 영어 메시지를 친절한 한국어로 변환 (예: 비밀번호 재설정 보안 제한 → "보안을 위해 N초 뒤에 다시 시도").

---

## 9. 개발/배포 워크플로

- **작업 브랜치**: `claude/tarot-app-dev-h9lhzg` 에서 개발.
- **흐름**: 커밋 → `main` 최신화(rebase) → 푸시 → PR 생성 → `main` 병합.
- 내 PC로 반영: 로컬 폴더에서 `git checkout main && git pull`.

```powershell
# 최신 내려받기
git checkout main
git pull

# 새 작업 시작 (예시)
git checkout -b feature/새기능
# ...코드 수정...
npm run lint && npm run build   # 확인
git add -A
git commit -m "설명"
git push -u origin feature/새기능
# GitHub에서 PR 생성 → main 병합
```

> 커밋 전에는 항상 `npm run build`(= `tsc -b && vite build`)와 `npm run lint`가 통과하는지 확인하세요.

---

## 10. 자주 겪는 문제 (FAQ)

- **"For security purposes, you can only request this after N seconds"**
  → 오류 아님. 비밀번호 재설정 메일은 보안상 **약 60초에 한 번**만 요청 가능. **1분 기다렸다 다시** 시도. (앱은 이 메시지를 한국어로 안내함)
- **재설정/인증 메일이 안 옴**
  → 스팸함 확인. Supabase 대시보드에서 이메일 발송(SMTP) 설정·발송 한도 확인.
- **`Supabase 환경변수가 없습니다` 에러**
  → `.env` 파일 없음/오타. 4~5번 항목대로 값 채우고 **개발 서버 재시작**.
- **사진이 안 올라감**
  → Storage에 `diary-photos` 버킷 존재 및 정책 확인.
- **로그인해도 남의 데이터가 보이거나 저장 안 됨**
  → 테이블 **RLS 정책**(`auth.uid() = user_id`) 확인.
- **`git pull` 충돌**
  → 충돌 메시지를 확인하고, 로컬에서 임의 수정한 게 없으면 `git stash` 후 `git pull`.

---

## 11. 관련 문서

- `기획서.md` — 초기 기획
- `소소다이어리-안내서.md` — 사용자 안내
- `타로-기능-안내.md` — 타로 기능 안내
- `마음한줄_구현프롬프트.md`, `world_quotes_1000.md` — 명언 관련 원본

---

_이 문서는 프로젝트 인수인계·재시작용 마스터 가이드입니다. 구조가 바뀌면 이 파일도 함께 업데이트하세요._ 🌿
