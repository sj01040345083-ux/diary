# 🌿 소소 다이어리 (Soso Diary) — 나만의 개발·운영 안내서

> 이 문서 하나만 있으면, 시간이 지나 앱을 다시 손봐야 할 때
> "이게 뭐였더라?" 하지 않고 바로 이어서 할 수 있도록 정리한 안내서예요.
> 개발을 잘 몰라도 읽을 수 있게 쉬운 말로 적었습니다.
>
> _최종 정리: 2026년 7월_

---

## 1. 이 앱은 무엇인가요?

**소소 다이어리**는 하루의 감정을 한 줄로 남기는 **감정 일기 + 간단 가계부** 웹 앱이에요.
휴대폰·PC 어디서든 인터넷 주소로 열 수 있고, 앱처럼 홈 화면에 추가해서 쓸 수 있어요.

### 접속 주소 (북마크 추천 ⭐)
```
https://diary-seojung.vercel.app
```
- 이 주소는 **항상 최신 버전**으로 연결되는 고정 주소예요.
- `diary-xxxxxxx-seojung.vercel.app` 처럼 중간에 이상한 글자가 있는 주소는 **그때 그 배포만** 가리키는 임시 주소라, 나중에 옛날 화면이 뜰 수 있어요. 위 고정 주소를 쓰세요.

---

## 2. 주요 기능

| 기능 | 설명 |
|------|------|
| 📔 감정 일기 | 오늘의 기분(이모지 8개) + 한 줄 일기 + 사진 첨부 |
| 📅 지난날 일기 | 일기 쓰기 화면의 달력으로 과거 날짜를 골라 그날 일기도 작성/수정 |
| 💰 소비·수입 기록 | 금액·카테고리별로 지출/수입 기록 (간단 가계부) |
| 📊 통계 | 수입·지출·잔액 요약, 도넛 그래프, 카테고리별 내역 |
| 🍀 오늘의 명언 | 매번 다른 명언 표시, 마음에 들면 즐겨찾기 |
| 📷 사진 | 일기에 사진 첨부 (여러 장 콜라주로 표시) |
| ⬇️ 내보내기 | 통계를 엑셀(.xlsx), 일기를 워드(.docx)로 저장 |
| ⚙️ 설정 | 닉네임, 글씨 크기·글씨체, 배경 사진 변경 |

### 화면 구성
- **홈** : 인사말 + 오늘 일기 쓰기/소비·수입 버튼 + 오늘의 명언 + **오늘 쓴 일기**
- **기록** : 달력 + **지난 일기 전체** (날짜 클릭 시 그날 사진도 표시)
- **통계** : 월별 수입·지출 요약과 그래프
- **설정** : 닉네임·글씨·배경 등

> 💡 홈에는 '오늘 일기'만, 지난 일기는 '기록' 탭에서 봅니다. (화면 역할을 나눠 깔끔하게)

---

## 3. 무엇으로 만들어졌나요? (기술 구성)

어려운 이름이 나오지만, 각각의 '역할'만 알면 충분해요.

| 이름 | 쉬운 설명 |
|------|-----------|
| **React + TypeScript** | 화면을 만드는 재료 (레고 블록 같은 것) |
| **Vite** | 재료를 조립해 실제 앱으로 만들어주는 도구 |
| **Supabase** | 로그인 + 데이터 저장소(서버). 일기·가계부·사진이 여기 저장됨 |
| **Vercel** | 앱을 인터넷에 올려 누구나 접속하게 해주는 곳 (배포) |
| **GitHub** | 코드(소스)를 보관하는 창고. 수정 기록이 다 남음 |

**흐름 한눈에:**
```
코드 수정 → GitHub에 저장 → Vercel이 자동으로 인터넷에 새 버전 배포
데이터(일기·가계부·사진) → Supabase 서버에 저장 (기기 바뀌어도 유지)
```

---

## 4. 폴더·파일 구조 (핵심만)

```
diary/
├── .env                  ← Supabase 비밀 주소·키 (GitHub에 안 올라감)
├── .env.example          ← .env를 어떻게 채우는지 예시
├── index.html            ← 앱의 껍데기 + 글꼴 불러오기
├── src/
│   ├── App.tsx           ← 앱 시작점 (로그인 확인 → 화면 결정)
│   ├── main.tsx          ← 최초 실행 파일
│   ├── index.css         ← 전체 색·배경 등 공통 디자인
│   │
│   ├── pages/            ← 각 '화면'
│   │   ├── HomePage.tsx       (홈)
│   │   ├── WritePage.tsx      (일기 쓰기 — 날짜 선택 달력 포함)
│   │   ├── RecordsPage.tsx    (기록 — 지난 일기)
│   │   ├── TransactionsPage.tsx (소비·수입 기록)
│   │   ├── StatsPage.tsx      (통계)
│   │   ├── SettingsPage.tsx   (설정)
│   │   ├── LoginPage.tsx / SignupPage.tsx (로그인/가입)
│   │   ├── home.css           (홈·일기·기록·통계 등 대부분 화면 디자인)
│   │   └── auth.css           (로그인 화면 디자인)
│   │
│   ├── components/       ← 여러 화면에서 쓰는 '부품'
│   │   ├── AppShell.tsx       (탭 전환·화면 덮기 담당)
│   │   ├── TabBar.tsx         (하단 탭바)
│   │   ├── DiaryCalendar.tsx  (기록 탭 달력)
│   │   ├── DatePickerCalendar.tsx (일기 쓰기 날짜 선택 달력)
│   │   ├── DiaryPhotos.tsx / PhotoCollage.tsx (사진 표시)
│   │   └── Header.tsx
│   │
│   ├── config/          ← ⭐ 내용을 바꾸고 싶을 때 여는 곳
│   │   ├── quotes.ts          (명언 목록)
│   │   ├── categories.ts      (가계부 카테고리·아이콘·색)
│   │   ├── emotions.ts        (기분 이모지)
│   │   ├── backgrounds.ts     (배경 사진 목록)
│   │   └── theme.ts / appConfig.ts
│   │
│   ├── lib/             ← 실제 '일하는' 코드 (서버 연결 등)
│   │   ├── supabase.ts        (서버 연결 통로)
│   │   ├── diaries.ts         (일기 저장/불러오기)
│   │   ├── transactions.ts    (가계부)
│   │   ├── photos.ts          (사진 — Supabase Storage)
│   │   ├── favorites.ts       (명언 즐겨찾기)
│   │   ├── settings.ts        (닉네임·글씨·배경 설정)
│   │   ├── today.ts           (날짜·명언 뽑기)
│   │   └── exporters.ts       (엑셀·워드 내보내기)
│   │
│   └── assets/backgrounds/  ← 배경 사진 이미지들(.jpg)
```

---

## 5. ⭐ 자주 바꾸고 싶은 것들 (어디를 고치면 되나)

### 5-1. 명언 추가·수정
- 파일: `src/config/quotes.ts`
- 방법: 배열에 `{ id: 111, text: "명언 내용", author: "저자" },` 형태로 한 줄 추가
- 규칙: **id는 겹치지 않게**, **text(문장)는 서로 다르게** (같은 문장을 여러 개 넣으면 "같은 것만 나온다"고 느껴져요)
- 현재 명언 수: **123개** (모두 서로 다른 문장)

### 5-2. 가계부 카테고리·아이콘 바꾸기
- 파일: `src/config/categories.ts`
- 지출 카테고리: `식비, 교통, 생활, 쇼핑, 문화, 건강, 기타`
- 수입 카테고리: `월급, 용돈, 부수입`
- 아이콘도 `categoryIcons`에서 이모지로 지정

### 5-3. 기분 이모지 바꾸기
- 파일: `src/config/emotions.ts` (현재 8개, 한 줄에 표시)

### 5-4. 배경 바꾸기/추가
- **기본 배경 = 밝고 깔끔한 단색** (사진 아님). 색은 `src/index.css`의 `body::before`에서 조정해요.
- **기본 제공 사진 배경**: 이미지 위치 `src/assets/backgrounds/` (여기에 `.jpg` 넣으면 자동 인식), 목록은 `src/config/backgrounds.ts` — 설정에서 고르면 기본 단색 위에 사진이 덮여요
- **내 사진 업로드** (사용자 기능): 앱의 **설정 → 배경 사진 → ＋ 내 사진** 버튼으로 직접 사진을 골라 배경으로 지정할 수 있어요.
  - 올린 사진은 자동으로 리사이즈/압축(긴 변 1600px, JPEG) 후 **base64(Data URL)** 로 변환돼 **그 기기의 localStorage(`soso.customBg`)** 에 저장됩니다. 새로고침·재접속해도 유지돼요.
  - **↩ 기본 배경으로 되돌리기** 버튼으로 언제든 기본 배경으로 복구할 수 있어요.
  - 관련 코드: `src/pages/SettingsPage.tsx`(UI), `src/lib/settings.ts`(저장/적용: `CUSTOM_BG`, `getCustomBg`/`setCustomBg`/`clearCustomBg`, `resolveBgUrl`), `src/lib/imageCompress.ts`(`fileToBackgroundDataUrl`)
  - 📌 내 사진 배경은 **기기별 저장**이라 다른 기기에는 동기화되지 않아요. (기본 제공 배경 선택은 Supabase에 동기화됨)
- ⚠️ 기본 배경 이미지를 폴더에 직접 넣을 땐 너무 크면(수 MB) 앱이 느려져요. **가로 1400px 정도, 200KB 안팎**으로 줄여서 넣는 게 좋아요.

### 5-5. 전체 색/글꼴 톤
- 색: `src/index.css` 맨 위 `:root { ... }` (초록·크림 등)
- 글꼴: `index.html` (Gowun Dodum / Gowun Batang)

---

## 6. 내 컴퓨터에서 실행·개발하기

> 새 컴퓨터에서 처음 시작할 때 순서예요.

1. **Node.js 설치** (한 번만) — https://nodejs.org 에서 LTS 버전
2. 코드 내려받기
   ```bash
   git clone https://github.com/sj01040345083-ux/diary.git
   cd diary
   ```
3. 필요한 재료 설치
   ```bash
   npm install
   ```
4. **`.env` 파일 만들기** — `.env.example`을 복사해 `.env`로 이름 바꾸고, Supabase 값 채우기
   ```
   VITE_SUPABASE_URL=https://xxxxx.supabase.co
   VITE_SUPABASE_PUBLISHABLE_KEY=(공개용 키)
   ```
5. 개발 서버 켜기
   ```bash
   npm run dev
   ```
   → 브라우저에서 `http://localhost:5173` 접속

### 자주 쓰는 명령어
| 명령어 | 하는 일 |
|--------|---------|
| `npm run dev` | 개발용 서버 켜기 (수정하면 바로 반영) |
| `npm run build` | 배포용으로 조립 + 오류 검사 (⭐ 올리기 전 확인) |
| `npm run lint` | 코드 문법 점검 |
| `npm run preview` | 조립된 결과 미리보기 |

---

## 7. 🔑 환경변수(.env)와 비밀 키 주의

- `.env` 파일은 **GitHub에 올라가지 않아요** (`.gitignore`에 등록됨). 비밀이니까요.
- 프론트엔드(브라우저)에서는 **Publishable(공개용) 키만** 사용해요.
- ⚠️ **`service_role` / `secret` 키는 절대** 코드나 공개 저장소에 넣지 마세요. (누구나 데이터를 조작할 수 있게 됨)

---

## 8. Supabase (데이터 서버) 구성

### 8-1. 사용 중인 데이터 테이블
| 테이블 | 저장 내용 |
|--------|-----------|
| `diaries` | 일기 (날짜, 내용, 기분) |
| `transactions` | 소비·수입 기록 |
| `favorite_quotes` | 즐겨찾은 명언 |
| `settings` | 닉네임·글씨·배경 등 개인 설정 |
| `categories` | 사용자가 추가한 커스텀 카테고리 |

### 8-2. 사진 저장 (Storage)
- 버킷 이름: **`diary-photos`**
- 사진은 `사용자ID/날짜/파일` 형태로 저장되고, 본인만 접근 가능(RLS)
- 📌 새 Supabase 프로젝트에서 사진 기능을 쓰려면 이 버킷을 만들고 RLS 정책(본인 폴더만 읽기/쓰기/삭제)을 넣어야 해요.

### 8-3. ⚠️ 무료 Supabase의 '일시정지' 주의 (중요!)
- 무료 플랜은 **약 1주일 아무도 접속 안 하면 프로젝트가 자동 일시정지(paused)** 돼요.
- 이때 앱에서 **"서버에 연결하지 못했어요"** 오류가 나요. (데이터는 안전해요!)
- **해결:** Supabase 대시보드 → 해당 프로젝트 → **"Resume project"** 버튼 클릭 (Upgrade 아님)

---

## 9. 배포 (인터넷에 새 버전 올리기)

### 자동 배포 흐름
```
코드 수정 → GitHub main 브랜치에 반영 → Vercel이 자동 감지 → 1~2분 뒤 새 버전 공개
```

### 브랜치 & PR이 뭔가요? (초간단)
- **브랜치** = 원본을 건드리지 않고 따로 수정하는 '연습장'
- **PR(Pull Request)** = "연습장 내용을 원본(main)에 합쳐도 될까요?" 하는 합치기 요청
- **Merge(머지)** = 실제로 합치기 → 합쳐지면 Vercel이 자동 배포
- 이 프로젝트의 작업 브랜치: `claude/app-structure-review-h8r7zv`

### 새 버전이 안 보일 때
휴대폰/PC에서 **새로고침** (당겨서 새로고침) 하면 최신 버전이 떠요.

---

## 9-B. 📲 PWA (앱처럼 설치하기 + 오프라인)

이 앱은 **PWA(Progressive Web App)** 라서, 브라우저에서 열고 **홈 화면에 설치**해 진짜 앱처럼 쓸 수 있어요. 한 번 열어두면 **인터넷이 없어도 앱 화면은 열려요.** (단, 일기 저장·불러오기는 인터넷 필요)

### 관련 파일
| 파일 | 역할 |
|------|------|
| `public/manifest.webmanifest` | 앱 이름·아이콘·테마색·`display: standalone`(주소창 없는 앱 모드) |
| `public/icons/icon-192x192.png`, `icon-512x512.png` | 설치 아이콘 |
| `public/sw.js` | **서비스 워커** — 앱 껍데기를 캐시에 저장해 오프라인에서도 열리게 함 |
| `index.html` | 위 manifest 링크 + `sw.js` 등록 코드 포함 |

### 휴대폰에 설치하는 법
- **아이폰(Safari)**: 공유 버튼 → **홈 화면에 추가**
- **안드로이드(Chrome)**: 메뉴(⋮) → **앱 설치** / **홈 화면에 추가**
- 설치하면 아이콘으로 실행되고, 주소창 없이 전체 화면으로 떠요.

### ⚠️ 서비스 워커 테스트는 '빌드+미리보기'로
서비스 워커는 **개발 서버(`npm run dev`)에서는 잘 동작하지 않을 수 있어요.** 반드시 아래처럼 테스트하세요.
```bash
npm run build      # 배포용으로 조립
npm run preview    # http://localhost:4173 에서 열림
```
확인 방법 (브라우저 개발자도구 F12):
1. **Application(애플리케이션) 탭 → Service Workers** : "activated" 표시 확인
2. **Application → Manifest** : 앱 이름·아이콘이 보이는지 확인
3. **Network 탭에서 "Offline" 체크 → 새로고침** : 앱 화면이 그대로 열리면 성공 ✅

### 서비스 워커를 고쳤을 때
`public/sw.js`의 캐시 방식을 바꾸면, 맨 위 `const CACHE = 'soso-diary-v1'` 의 버전을 **v2, v3…** 로 올리세요. 그래야 사용자 기기의 옛 캐시가 자동으로 정리됩니다.

### 배포
PWA 파일들(`manifest`, `sw.js`, 아이콘)은 `public/` 폴더에 있어 **빌드 시 자동으로 포함**돼요. 평소처럼 GitHub main에 합치면 Vercel이 배포하고, 설치·오프라인 기능도 함께 적용됩니다.

---

## 10. 🚑 자주 겪는 문제 & 해결

| 증상 | 원인 | 해결 |
|------|------|------|
| "서버에 연결하지 못했어요" (로그인 안 됨) | Supabase 프로젝트 일시정지 | 대시보드에서 **Resume project** 클릭 |
| 앱이 계속 "불러오는 중…" | 서버 응답 지연 | 잠시 뒤 새로고침 (6초 후 자동으로 넘어가게 처리돼 있음) |
| 화면이 옛날 그대로 | 브라우저 캐시 | **새로고침** |
| 브라우저에선 사진이 안 보임 | Storage 버킷/RLS 미설정 | `diary-photos` 버킷 + RLS 정책 설정 |
| 명언이 계속 같은 것만 | (과거) 중복 명언 문제 | 이미 해결됨 (서로 다른 명언 123개) |
| 앱이 느려짐 | 배경 사진이 너무 큼 | 배경 이미지를 작게(200KB 안팎) 줄이기 |

---

## 11. 데이터는 안전한가요? (백업)

- 일기·가계부·사진·설정은 모두 **Supabase 서버**에 저장돼요. 기기를 바꿔도 로그인하면 그대로 있어요.
- 코드를 수정하거나 배포해도 **데이터는 지워지지 않아요.**
- 걱정되면: 앱의 **통계 → 엑셀 내보내기**, **일기 → 워드 내보내기**로 내 기록을 파일로 저장해 둘 수 있어요.
- ⚠️ Supabase 프로젝트 자체를 **삭제**하면 데이터도 사라져요. 일시정지(paused)는 삭제가 아니니 안심하세요.

---

## 12. 지금까지 해온 작업 (개발 히스토리 요약)

1. 앱 전체 구조 파악 + 디자인 톤 정리 (숲·아이보리·초록 자연 테마)
2. 배경 사진 자동 로드 + 하단 카드형 탭바 + 중앙 정렬(모바일 감성)
3. 로그인 화면 개선 + 자동 로그인
4. 일기: 기분 이모지(8개 한 줄), 이모티콘 넣기, 사진 첨부, 지난날 달력
5. 사진 저장을 기기 전용(IndexedDB) → **Supabase Storage**로 바꿔 여러 기기에서 보이게
6. 가계부: 소비·수입 기록/수정, 카테고리·아이콘, 수입 카테고리 정리
7. 통계: 수입·지출·잔액 요약, 도넛 그래프, 카테고리 내역
8. 엑셀(.xlsx) 내보내기(통계) · 워드(.docx) 내보내기(일기: 제목·날짜·본문·**첨부 사진**까지, 한 편/전체 선택)
9. 성능: 배경 깜빡임 제거, 사진 조회 최적화, 시작 로딩 무한대기 방지
10. 홈 화면 정리: 버튼을 위로, 인사말~명언 간격 촘촘히, 한 화면에 맞춤
11. **명언 중복 정리(1000→110개) + 연속 반복 방지**
12. **홈에는 '오늘의 일기'만, 지난 일기는 '기록' 탭에서만** 표시

---

## 13. 앞으로 할 수 있는 것 (선택)

- **카카오 로그인**: 코드는 준비돼 있고, 카카오 개발자 콘솔 + Supabase 제공자 설정만 완료하면 켤 수 있어요.
- **사진 기능 완성**: 새 환경에서 `diary-photos` 버킷 + RLS 정책 설정.
- 명언 더 추가, 배경 사진 추가, 카테고리 조정 등은 `src/config/` 파일에서 언제든 가능.

---

## 14. 한눈에 보는 요약 카드 📌

- **접속 주소**: https://diary-seojung.vercel.app
- **코드 저장소**: https://github.com/sj01040345083-ux/diary
- **데이터 서버**: Supabase (일주일 방치 시 일시정지 → Resume로 복구)
- **배포**: GitHub main에 합치면 Vercel이 자동 배포 (1~2분)
- **내용 바꾸기**: `src/config/` 폴더 (명언·카테고리·배경·이모지)
- **올리기 전 확인**: `npm run build` 오류 없는지 확인

---

_이 안내서는 프로젝트가 바뀌면 함께 업데이트하면 좋아요. 🌿_
